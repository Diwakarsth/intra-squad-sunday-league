# Free Gallery Setup

No Firebase Storage or billing upgrade is needed.

## One-time Firebase step
Firebase Console → Firestore Database → Rules → publish the included `firestore.rules`.

## Add media
Admin → Gallery → choose match → Photo/Video → paste public link → optional caption → Add to Match Gallery.

Use YouTube for videos and a direct public HTTPS image link for photos.
